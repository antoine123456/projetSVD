#include <SVD.h>

// eigen_t SVD_1(float *A, int m, int n) {
//     float *B;

//     // Compute A^TA or AA^T depending on the comparaison between n and m
//     if (n < m)
//         B = get_AtA(A, m, n);
//     else
//         B = get_AAt(A, m, n);

// }